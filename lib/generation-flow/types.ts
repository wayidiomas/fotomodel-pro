export interface CategoryTip {
  goodDescription: string;
  goodExamples: string[]; // Array of image URLs
  avoidTitle: string;
  avoidItems: string[]; // Array of bullet points
  badExamples: string[]; // Array of image URLs
}
